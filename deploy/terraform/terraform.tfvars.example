# The AWS provider uses the active AWS CLI profile and configured region.
# Supply the API key only through the environment:
# export TF_VAR_temporal_api_key='...'

project_name       = "temporal-word-game"
domain_name        = "games.tmprl-demo.cloud"
hosted_zone_name   = "tmprl-demo.cloud"
temporal_namespace = "michaelj-durable-games.a2dd6"
temporal_address   = "michaelj-durable-games.a2dd6.tmprl.cloud:7233"
